{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file", // Changed label to reflect g++
            "command": "C:/mingw64/bin/g++.exe", // <--- CRITICAL: Changed from gcc.exe to g++.exe
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "${file}", // Compiles the currently active C++ file
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe" // Puts the .exe next to the source file
            ],
            "options": {
                "cwd": "${fileDirname}" // <--- CRITICAL: Changed cwd to the directory of the source file
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger (Modified for C++ compilation)."
        }
    ],
    "version": "2.0.0"
}